[build]
builder = "nixpacks"

[[services]]
name = "call-recorder-api"
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"

[[services]]
name = "lead-cron"
cronSchedule = "0 13 * * 1-5"   # 7:00 AM CST (UTC-6) Mon-Fri
startCommand = "python apify_cron.py"
